# Elliott Equipment — Progress Log

> **Newest entries at the top. Updated every session.**
>
> **Rolling window: 10 entries max. When adding an 11th, remove the oldest. Git retains all history.**
>
> This file is the session memory layer: why decisions were made, what changed strategically, what a future session needs to know. It is not a commit log and not a validation archive — full validation records live in `items/*.md` (Pricing Derivation), file-level changes live in git history, and structure/math compliance is enforced by `scripts/validate.py`. Entry format (template in `.claude/COMPLETION_TEMPLATES.md`): What / Key Decisions / Strategic Flags / Status, 10–25 lines per entry, no other sections.
>
> Last Updated: 2026-08-10 (Session BK — new item: P/N 101903A, LBL-BM EXT SELECT DIGGER, filed via direct match to governing comparable P/N 3024140. 2.0" × 1.625" = 0.023 sq ft, non-ANSI simple control label, sub-0.06 sq ft per-label floor governs. Priced by Nick's manual routing — calculator flagged F26 STOP and confirmed 3024140 as the correct non-ANSI governing comparable; tier table byte-identical to 3024140 ($4.00/$3.00/$2.50/$2.25/$2.00/$1.75); material cost $0.10/label (§25 canonical + incidental buffer); not run through calculator or 4-wave AI validation. Item count 62 → 63; Printed/Laminated category 44 → 45 items; scripts/validate.py BAND_EXCEPTIONS entry added. Previously Session BJ — P/N 127763A filed via Direct Parity Exemption from 1062390, sub-scope at 0.4861 sq ft, §31 deviation flagged not corrected. Previously Session BI — structural fix: migrate_to_supabase.py MCP fallback. Previously Session BH — data-layer fix: 1277630/3024180 Supabase sync gap. Previously Session BG — 1277630 §32 gap resolved as One-Time Exception; 3024180 added at parity with 1012080. Previously Session BF — 1277630 added as seventh singles-band data point. Previously Session BE — 1001530 orphan-row investigation. Previously Session BD — §32 amendment. Previously Session BC — 1257750 added, direct price replication of 3010736. Previously Session BB — 3023921 added, full 4-wave validation.)

---

### 2026-08-10 — Session BK (new item): P/N 101903A — LBL-BM EXT SELECT DIGGER, sub-0.06 sq ft non-ANSI control label, per-label floor governs, direct match to P/N 3024140

**What:** New printed/laminated single label at 2.0" × 1.625" = 0.023 sq ft. Priced via direct match to P/N 3024140 (0.019 sq ft, non-ANSI simple control label, $2.50 at qty 20) per Nick's manual routing on 2026-08-10. The calculator's own F26 STOP flag (sub-0.06 sq ft, per-label floor governs) independently confirmed 3024140 as the correct non-ANSI governing comparable — Nick's manual match is consistent with the calculator's routing. NOT run through the calculator or 4-wave AI validation. Tier table byte-identical to 3024140 across all six tiers: $4.00/$3.00/$2.50/$2.25/$2.00/$1.75. Pricing already quoted to Sean.

**Key Decisions:**
- **Per-label floor governs, not $/sq ft.** At 0.023 sq ft the linear Micro-Format Band formula ($30.86/sq ft × 0.023 = $0.71) is inapplicable. The implied $/sq ft ($108.70 at qty 20) is a mathematical artifact, not the pricing basis. The governing number is $2.50 per label at qty 20 — the complexity-dependent non-ANSI floor established by 3024140.
- **Material cost $0.10/label** — §25 canonical: Orajet 0.023 × $1.21 = $0.0278 + lam 0.023 × $0.2389 = $0.0055 + ink 0.023 × $0.50 = $0.0115 = $0.0448 calculated + $0.0552 incidental buffer = $0.10. Same filed cost as 3024140; material is negligible at this size class.
- Fourth sub-0.06 sq ft production item on the account (after 3024592, 3024140, 1205870). `do_not_benchmark = false` (valid complexity-floor data point).
- `categories/printed-laminated-orajet.md` updated: items table row + footnote ³⁶ added. `.claude/ARCHITECTURE.md` catalog row added; Category Registry Printed + Laminated updated (44 → 45 items). `materials/orajet-3951-white.md` and `materials/1mil-polyester-overlaminate.md` `used_in_items` arrays both updated. `scripts/validate.py` BAND_EXCEPTIONS entry added.

**Strategic Flags:**
- Pricing is already quoted to Sean — do not alter. No override_type (no rule was overridden; the per-label floor is the governing logic, and the price matches it exactly).
- First article not requested. Order quantity not confirmed with Sean.
- Margin figures are material-only ($0.10/label basis) — no fully-loaded cost doctrine exists for this account.
- Item count: 62 → 63. Printed/Laminated category: 44 → 45 items.

**Status:** Complete — `items/101903A.md`, category file, `.claude/ARCHITECTURE.md`, `scripts/validate.py`, both material cross-refs, `.claude/STATE.yml`, and this entry all updated. `python scripts/validate.py` — 0 errors, 0 warnings (1 informational band-exception entry for 101903A, expected). `python scripts/build_frontend.py` — 63 items. `build_materials.py` — 14 materials. `build_calculator_config.py` — clean. Supabase synced via the credential-free MCP fallback: upsert SQL executed via `mcp__Supabase__execute_sql` against the `prolabel` project (tszssadgsxjoymcttlwd), confirmed via direct query — 101903A present with correct data, `material_combination_id` linked; 64 total `elliott_items` rows (63 repo items + 1 known 1001530 orphan).

---

### 2026-08-03 — Session BJ (new item): P/N 127763A — CHART-V63 V1 METRIC AERIAL OPERATION, Direct Parity Exemption from P/N 1062390, sub-scope classification (below the ~0.5 sq ft singles-band floor), §31 deviation flagged

**What:** New printed/laminated single label at 7.0" × 10.0" = 0.4861 sq ft, Model V63. Read `items/1062390.md` in full before filing to confirm material family (Orajet 3951 Cast + Polyester Lam), process (Print/Lam/Cut, 1 pass), and ANSI status (§29 default) — this item matches all three. Filed via **Direct Parity Exemption** from P/N 1062390 (7.25" × 10.00" = 0.503 sq ft, ~3.4% larger, same material/process/ANSI class): tier table is Nick-locked and was already quoted to the customer prior to this session — byte-identical to 1062390, $16.50/$10.50/$8.00/$6.25/$5.25/$4.25, filed as given with no derivation, scaling, or validation run against it.

**Key Decisions:**
- **Sub-scope classification, not a new band data point.** At 0.4861 sq ft this item sits below the ~0.5 sq ft singles-band scope floor that governing benchmark 1062390 (0.503 sq ft) sits inside — it routes sub-scope per the category decision tree. It is NOT a new singles-band data point (unlike 1062390 itself), and it is also NOT a new independent sub-scope data point — this is a parity-clone filing of a standard-band item's price, not a fresh sub-scope validation, mirroring how direct-parity clones elsewhere on the account (e.g., 3020336/3020482 vs. 3020335) are excluded from data-point counts. Added to `scripts/validate.py` `BAND_EXCEPTIONS`.
- **Material cost independently calculated, not cloned.** $0.95/label, computed fresh this session via the §25 canonical formula for this item's own 0.4861 sq ft footprint (Orajet $0.5882 + lam $0.1161 + ink $0.2431 = $0.9474 calculated + incidental buffer = $0.95) — explicitly NOT copied from 1062390's filed $0.98/label, which reflects 1062390's own, larger 0.503 sq ft area.
- **§31 sub-scope root floor NOT applied — deviation flagged, not corrected.** At this item's own 0.4861 sq ft, the 50-99/100-199/200+ tiers ($12.86/$10.80/$8.74 per sq ft) price below the `governance/PRICING_RULES.md` §31 floor ($15.43/sq ft at every tier). Per explicit session instruction, §31 floor recalculation is out of scope for this filing — the pricing is Nick-locked and already quoted to the customer. This is documented in `items/127763A.md` Pricing Derivation Step 4 and category footnote ³⁵ for Nick's awareness, not auto-corrected. `override_type` filed as `"Direct Parity Exemption"` (not a One-Time Exception) — the underlying decision was cloning a standard-band benchmark's price onto a sub-scope-sized label, not overriding an engine recommendation.
- At the qty-20 tier, $8.00/0.4861 sq ft = $16.46/sq ft — above the singles band ceiling ($15.91/sq ft), consistent with the account's sub-scope small-format-premium doctrine (expected direction, not a concern).
- `categories/printed-laminated-orajet.md` updated: items table row + footnote ³⁵ added; sub-scope Pricing Profile prose updated to reference 127763A explicitly, without incrementing either the singles-band or sub-scope data-point counts. `materials/orajet-3951-white.md` and `materials/1mil-polyester-overlaminate.md` `used_in_items` arrays both updated. `.claude/ARCHITECTURE.md` catalog row added; Category Registry Printed + Laminated row updated (43 → 44 items).

**Strategic Flags:**
- This is the first item on file where a Direct Parity Exemption clone crosses the singles-band/sub-scope boundary that its own benchmark does not — future sessions filing similarly-sized parity clones of standard-band items should expect the same sub-scope routing and §31 flag pattern.
- The §31 deviation on the deep tiers is a documented, Nick-directed decision (pricing already quoted to the customer) — not a data error. Do not "fix" it in a future session without Nick's explicit direction, consistent with how the account handles other locked-but-below-floor tiers (cf. 1101250, 3017572).
- First article not yet requested — flagged for follow-up, does not block sending. Order quantity not yet confirmed.
- Item count: 61 → 62. Printed/Laminated category: 43 → 44 items.

**Status:** Complete — `items/127763A.md`, category file, `.claude/ARCHITECTURE.md`, `scripts/validate.py`, both material cross-refs, `.claude/STATE.yml`, and this entry all updated. `python scripts/build_frontend.py` (62 items), `build_materials.py`, and `build_calculator_config.py` all run clean. `python scripts/validate.py` — 0 errors, 0 warnings. Supabase synced via the credential-free MCP fallback: `python scripts/migrate_to_supabase.py --pn 127763A --verify` emitted the scoped upsert SQL, executed directly via `mcp__Supabase__execute_sql` against the `prolabel` project, then confirmed via `--dry-run --verify` (VERIFY OK) and `--verify --all` (62/62 repo items present; only the pre-existing documented 1001530 orphan remains as the sole discrepancy).

---

### 2026-07-30 — Session BI (structural fix): Supabase sync made credential-independent and mandatory — `migrate_to_supabase.py` MCP fallback, `.claude/COMPLETION_TEMPLATES.md` checklist updated

**What:** Session BH's fix (2026-07-30, same day) closed the immediate gap on 1277630/3024180 but was itself another one-off manual SQL patch — the fourth time this exact failure mode has hit the account (1257750, 1001530, 1277630, 3024180). This session closes the process gap instead of the symptom. Audited `.claude/COMPLETION_TEMPLATES.md`: the Supabase sync step existed (Update Triggers table row + Session Completion item 5) but instructed "if credentials are missing, create the `.env` file first" — unworkable, since Claude Code sessions never have a service-role key to put in it, which is exactly why every new-item session for the last several months has silently deferred the sync.

**Key Decisions:**
- **`scripts/migrate_to_supabase.py` now has a credential-free MCP fallback.** Added `--pn [PN]` (scopes a run to specific part numbers, comma-separated or repeated) and `--verify` (confirms via an anon-key REST read — RLS already grants anon SELECT on `elliott_items`, no service-role key needed — that the row(s) exist). When `SUPABASE_SERVICE_ROLE_KEY` is present, behavior is unchanged (full live sync via `supabase-py`). When it is absent — the normal case in this environment — and `--pn` is given, the script prints the exact scoped upsert SQL (`elliott_items` + `elliott_items_internal` + the `material_combination_id` link, nothing else) for just those P/Ns and instructs the session to execute it via `mcp__Supabase__execute_sql`, then re-run with `--verify`. Added `emit_scoped_sql()` (mirrors `emit_sql()`'s idempotent upsert logic but limited to the passed-in row subset) and `run_live_items_only()` (the credentialed equivalent) so the scoped path never touches materials/bands/settings/combination-definition rows or any other item's row.
- **`--verify --all`** (no `--pn`) checks every `items/*.md` P/N against `elliott_items` with no writes — run this session per Task 4: **61/61 repo items confirmed present.** The only discrepancy is the pre-existing, already-investigated P/N 1001530 orphan row (Session BE, 2026-07-22) — 62 total `elliott_items` rows (61 repo + 1 orphan), hardcoded as `KNOWN_ORPHAN_PNS` so `--verify --all` reports it as documented, not a failure. Not fixed this session, per the task's explicit scope.
- **`.claude/COMPLETION_TEMPLATES.md` rewritten**: the "New item quoted" and "Supabase seed required" trigger-table rows now point at `--pn [PN] --verify`; a new **Mandatory Supabase Sync** section states plainly that a session filing an item without a confirmed sync is incomplete — do not commit, do not push, do not mark done — and documents the MCP fallback step-by-step so no future session re-derives this from scratch or defers to Nick. Session Completion item 5 rewritten to match (drops the "create `.env`" instruction entirely).

**Strategic Flags:**
- **This should be the last time this failure mode requires a manual after-the-fact fix.** Every future new-item session now has a working, credential-independent path to a verified sync in the same session it files the item — the excuse that caused Sessions BF/BG (and, further back, whatever produced the 1001530 orphan) to defer is gone.
- The 1001530 orphan is intentionally left unresolved — it needs Nick's reconciliation decision (backfill a repo item file from the Supabase record, or correct the record), not a script fix. `--verify --all` will keep reporting it as documented until that decision is made and the orphan list is either cleared or a real `items/1001530.md` is authored.
- No item pricing, item files, or category files were touched this session — this was tooling + governance only.

**Status:** Complete — `scripts/migrate_to_supabase.py` updated (`--pn`, `--verify`, `--all`, MCP-fallback SQL emission, anon-key verification); `.claude/COMPLETION_TEMPLATES.md` updated (mandatory sync checklist + MCP fallback documented); `python scripts/migrate_to_supabase.py --verify --all` — 61/61 repo items confirmed present, only the known 1001530 orphan flagged; `python scripts/validate.py` — 0 errors, 0 warnings.

---

### 2026-07-30 — Session BH (data-layer fix): diagnosed and fixed why P/N 1277630 and P/N 3024180 weren't visible on the Vercel frontend — Supabase `elliott_items` sync gap, not a repo/build/deploy problem

**What:** Investigated a report that P/N 1277630 and P/N 3024180 were not visible on the live Vercel frontend despite both being committed to `main` and built into `frontend/data.json`. Ruled out all three hypotheses in the requested checklist: (1) `frontend/data.json` is **not** gitignored — `.gitignore` only excludes Python/OS cruft; (2) `git show origin/main:frontend/data.json | grep -E "1277630|3024180"` confirmed both items **are** present in the committed file; (3) `mcp__Vercel__list_deployments` confirmed both the commit-`8aabd76` deployment and the subsequent auto-rebuild deployment (`2cc63db`) are `state: READY`, `target: production` — no build failure. **Root cause was architectural, not a repo/build/deploy defect:** per `frontend/index.html`, the deployed app reads live item data from Supabase's `elliott_items` table first (`DATA_SOURCE = 'supabase'`) — `frontend/data.json` is fetched only for prose sections and as an offline fallback. Querying `elliott_items` directly (`mcp__Supabase__execute_sql`) confirmed the table had 60 rows and was missing exactly `1277630` and `3024180` — neither item had ever been migrated to Supabase, because the two sessions that filed them (Session BF, Session BG) both lacked local `SUPABASE_SERVICE_ROLE_KEY` credentials for `scripts/migrate_to_supabase.py` and deferred the sync to Nick, per the account's long-standing pattern (documented in nearly every recent session's Status line).

**Key Decisions:**
- **Fixed via direct Supabase MCP `execute_sql` access** (available in this session, unlike the local-script path used by prior sessions) rather than waiting for Nick to run the migration script manually — consistent with the precedent already set on P/N 1257750 and P/N 1001530, both of which were synced the same way in earlier sessions per their own Progress entries.
- **Scoped the fix narrowly, not a full re-migration.** Ran `python3 scripts/migrate_to_supabase.py --emit-sql` locally (no credentials needed for this mode — it only reads repo files and prints SQL) to generate the exact, schema-correct upsert rows for these two items only, then extracted and executed just those two items' `elliott_items` and `elliott_items_internal` upsert statements plus the `material_combination_id` link update — not the full 60-item resync the script would otherwise emit. This avoids touching any of the other 59 already-synced rows.
- **Verified before and after:** a full `part_number` diff between the repo's `items/*.md` file list and the DB's `elliott_items` rows showed exactly two gaps (1277630, 3024180) and one known, unrelated extra (1001530, the previously-investigated real-but-unfiled orphan row — untouched). After the fix, the diff is clean except for that same, already-understood 1001530 case. `elliott_items` row count: 60 → 62 (60 + the two new rows; 1001530 was already counted in the "before" 60).
- Confirmed post-fix via direct query: both `1277630` and `3024180` now have rows in `elliott_items` (public/anon-readable columns, `pricing_logic`/`notes` correctly nulled per the account's D4 exception) and `elliott_items_internal` (service-role-only, carrying the real `pricing_logic`/`notes` text), and both have `material_combination_id` linked to the standard Orajet 3951 + 1-mil Poly Lam combination.

**Strategic Flags:**
- **Process gap, not fixed this session:** every new-item session on this account that lacks local service-role credentials silently defers the Supabase sync to "Nick to run manually" — this is the third or fourth time in the account's history that deferred sync has caused a visible discrepancy between the repo (source of truth) and the live app (Supabase-backed). Worth a future session investigating whether Supabase MCP access can become the default sync path instead of the credential-gated local script, to close this gap permanently.
- No repo files were changed this session — this was a Supabase-only data fix. `scripts/build_frontend.py` and `scripts/validate.py` were not re-run since no `items/*.md` content changed.
- Both P/N 1277630 and P/N 3024180 should now render correctly on the live Vercel frontend on next page load (no new deploy needed — Supabase reads are live, not build-time).

**Status:** Complete — `elliott_items` and `elliott_items_internal` both confirmed to contain 1277630 and 3024180 with correct data, `material_combination_id` linked. No git changes this session.

---

### 2026-07-30 — Session BG (cleanup + new item): P/N 1277630 merge confirmed, §32 gap resolved as One-Time Exception; P/N 3024180 added at exact parity with P/N 1012080

**What:** Three-task cleanup session. **Task 1** — checked whether branch `claude/item-1277630-pricing-fw723z` had been merged to main; `mcp__github__create_pull_request` returned "No commits between main and claude/item-1277630-pricing-fw723z," confirming the branch was already merged (PR #97, prior to this session) with an auto-rebuild commit on top. Fast-forwarded local `main` to `origin/main` — no new merge action needed. **Task 2** — re-examined the §32 chain-consistency flag on P/N 1277630 (filed Session BF) against its true nearest sq ft neighbor P/N 1277020 (0.635 sq ft), confirmed the gap still holds (50-99/100-199/200+ tiers: this item's $/sq ft exceeds 1277020's by ~3-4 $0.25 increments, past the 2-increment tolerance), and formally classified it `override_type: "One-Time Exception"` per Core Rule 12/13 — non-precedent-setting, Nick's locked tier table ($18.25/$16.00/$13.50/$12.50/$11.75/$11.00) left completely unmodified. **Task 3** — added P/N 3024180 (LBL-EMERGENCY STOP), 3.25" × 3.25" = 0.073 sq ft, a Micro-Format Band item priced at exact parity with P/N 1012080 (0.077 sq ft, size ratio 0.948, identical Orajet 3951 + polyester lam construction and process). No AI validation waves run — the parity match itself is the validation methodology. Filed tiers: $3.75/$2.90/$2.50/$2.20/$1.95/$1.90, byte-identical to 1012080.

**Key Decisions:**
- **Task 2 resolution: classify, don't re-price.** Per explicit session instruction not to change the anchor or any tier without flagging first, and since the price was independently locked via a full 4-wave, 6-model external validation against a different (rate-monotonicity) check, the §32 gap is closed by formal classification rather than by re-deriving the deep tiers. This mirrors the account's existing One-Time Exception precedent on P/N 1101250 and P/N 3017572 (both locked prices that price below a governing rule's floor, formally excepted rather than corrected). Distinct from P/N 3023921 and P/N 1257750, whose §32 gaps remain open/unresolved on the account's flagged list — 1277630 is now closed, those two are not.
- **Override reason logged in both the item file and category footnote:** the tier table was validated and locked against the session brief's stated rate-monotonicity bracket (1230820 upper / 1267140 lower); the §32 gap against 1277020 was discovered only in this later cleanup session and does not reopen the already-locked, externally-validated price. Future items must not benchmark their deep tiers against 1277630 as if §32-compliant against 1277020 — 1277020's own tier table remains the governing floor for that size range.
- **3024180's ANSI/non-ANSI classification mismatch flagged, not corrected.** The item is ANSI-marked (LBL-EMERGENCY STOP) per its spec sheet, but Nick's locked price is exact parity with P/N 1012080, a non-ANSI controller/panel label — every other ANSI item at a comparable size on this account (e.g. 3024592, ANSI Z535.6 DANGER) prices at or above the non-ANSI floor for its size class, not at parity with it. Documented plainly in the item file's dedicated "ANSI / Non-ANSI Classification Flag" section and in the category footnote, per the session's explicit instruction to file the Nick-approved parity price as directed.
- Calculator F7 flag (per-sq-ft rate outside ±15% Micro-Format Band tolerance) acknowledged on 3024180 as expected, correct behavior for a parity-governed item — same treatment already established for 1012080; added a matching `scripts/validate.py` BAND_EXCEPTIONS entry.
- `categories/printed-laminated-orajet.md` updated: items table row + footnote ³⁴ for 3024180; footnote ³³ and Pricing Profile prose updated to reflect 1277630's resolved §32 status; Micro-Format Band data-points prose updated for the second parity-governed item. `.claude/ARCHITECTURE.md` catalog row + Category Registry updated (Printed + Laminated 42 → 43 items). `materials/orajet-3951-white.md` and `materials/1mil-polyester-overlaminate.md` `used_in_items` arrays updated for 3024180.

**Strategic Flags:**
- **§32 flagged-list status after this session:** P/N 1277630 — RESOLVED (One-Time Exception). P/N 3023921 and P/N 1257750 — still OPEN, unresolved. Future cleanup sessions should continue working through the open pair.
- **3024180's ANSI/non-ANSI parity flag is a genuine open question for Nick**, not a data error — a future session should not treat this parity as precedent for pricing other ANSI items at non-ANSI-family rates without explicit direction.
- First article not yet requested by Sean on 3024180 — flagged for follow-up. Order quantity not specified — standard 6-tier ladder quoted.
- Item count: 60 → 61. Printed/Laminated category: 42 → 43 items.

**Status:** Complete — items/1277630.md, items/3024180.md, category file, ARCHITECTURE.md, PROGRESS.md, STATE.yml, materials cross-refs, and scripts/validate.py all updated. `scripts/build_frontend.py` run (61 items). `scripts/validate.py` run — 0 errors, 0 warnings. Committed and pushed directly to `main` per this session's explicit instruction (Task 4) — both items live on Vercel after this push.

---

### 2026-07-29 — Session BF (new item): P/N 1277630 — CHART-V63 V1 METRIC AERIAL & MAIN HOIST OPERATION, seventh singles-band data point, price pre-locked via external 4-wave/6-model AI validation, §32 chain-consistency gap flagged against true nearest neighbor

**What:** New printed/laminated single ANSI label at 12.5" × 10" = 0.868 sq ft, Model V63 — a genuinely new sq ft position in the singles band (≥0.5 sq ft), sitting between P/N 1277020 (0.635 sq ft) and the P/N 1230820 root benchmark (1.296 sq ft). Both label dimensions independently clear the 13.5" laminator (10" and 12.5", both under 13.5") — orientation is a nesting choice (10" across the web), not a forced constraint; 1 lamination pass. As with several recent sessions, the price was **not** derived by Claude Code: Nick ran a full 4-wave, 6-model AI validation process externally in ChatHub (24 independent model passes) 2026-07-29, synthesized the result in Claude Chat, and locked the final table before this session began. This session's job was to document the validation record in full and sync every dependent file. Filed tiers: $18.25/$16.00/$13.50/$12.50/$11.75/$11.00 — $13.50 at qty 20 = $15.55/sq ft, satisfying the session brief's rate-monotonicity bracket between P/N 1230820 ($15.43/sq ft, 1.296 sq ft) and P/N 1267140 ($15.62/sq ft, 0.560 sq ft).

**Key Decisions:**
- **Material cost carries a non-standard 20% multiplicative waste buffer.** The session brief specified Wave 2 corrected the filed material cost from the raw §25 calculated total ($1.6917 → $1.69) to a 20%-buffered total of **$2.03/label** — judged necessary for this label's contour-cut cast vinyl + polyester lamination sequence, which the validation found carries materially higher scrap risk (registration pulls, lamination misfeed on the 2-up print layout) than the flat-label baseline the standard cents-level incidental buffer is calibrated against. This is the only singles-band item on file with a buffer this large; documented explicitly in both the item file and the category profile so it isn't mistaken for the new band-wide convention.
- **Rate monotonicity constraint (as scoped by the session brief) is satisfied — $13.50 is the only $0.25-increment anchor that holds $15.55/sq ft between the $15.43/sq ft and $15.62/sq ft brackets.** However, **the session brief's chosen lower bracket (1267140, 0.560 sq ft) is not this item's true nearest sq ft neighbor** — that is P/N 1277020 (0.635 sq ft), a closer and independently-validated singles-band item. Running the `governance/PRICING_RULES.md` §32 Direction-B chain-consistency check against 1277020 (the correct comparison target) surfaced a real gap: the **50-99, 100-199, and 200+ tiers all invert** — this item's $/sq ft exceeds its smaller neighbor 1277020's $/sq ft at those three tiers, each by roughly 3–4 $0.25 increments, past the rule's 2-increment tolerance. **Flagged, not corrected** — per this session's scope of documenting a Nick-locked, externally-validated price rather than re-deriving it, and consistent with the account's established practice on P/N 3023921 and P/N 1257750. `override_type` left blank per the session brief's "Override classification: none" — the flag is a §32 finding independent of the override question, since the price is band-consistent by the metric the session was scoped to check.
- Both label dimensions independently clear the 13.5" laminator (unlike several smaller singles-band items where orientation is forced) — 1 pass, a nesting choice, not a constraint.
- `.claude/ARCHITECTURE.md` catalog row + Category Registry updated (Printed + Laminated 41 → 42 items, singles-band data points 6 → 7). `categories/printed-laminated-orajet.md` items table row + footnote ³³ added; Pricing Profile singles section updated (6 → 7 confirmed data points at 0.503/0.560/0.609/0.625/0.635/0.868/1.296 sq ft), metrics table (material cost/sq ft, selling price/sq ft, margin, tier compression, margin floor) and band-width prose all updated to include this item and its flagged §32 gap.
- `scripts/build_frontend.py` run to regenerate `frontend/data.json`. `scripts/validate.py` run — 0 errors, 0 warnings.

**Strategic Flags:**
- **This is the fourth item on file with a documented, unresolved §32 chain-consistency flag** (joining P/N 3023921, P/N 1257750, and the pre-existing "true one-off" list) — Nick should schedule a dedicated session to work through the accumulating flagged-not-corrected list rather than let it grow indefinitely.
- **Margin at this item (~84.9% at qty 20, ~81.5% at 200+) sits below the rest of the singles band (~82.6–88%)** solely because of the 20% waste buffer on material cost, not a pricing weakness — do not read this as an underpriced item in future comparative analysis.
- First article not yet requested by Sean — flagged for follow-up, does not block sending. Order quantity not specified — standard 6-tier ladder quoted.
- Item count: 59 → 60. Printed/Laminated category: 41 → 42 items. Singles-band data points: 6 → 7.

**Status:** Complete — item file, category file, ARCHITECTURE.md, STATE.yml updated; `scripts/build_frontend.py` run (60 items); `scripts/validate.py` run — 0 errors, 0 warnings. `scripts/migrate_to_supabase.py` NOT run in live mode — no `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` credentials available in this session environment (same deferral as prior sessions); Nick to run manually to bring `elliott_items` to 60 rows.

---

### 2026-07-22 — Session BE (investigation, no changes to Supabase): P/N 1001530 orphan-row investigation — NOT migration debris, real quoted item, session stopped for Nick's review

**What:** Investigated the suspected orphan Supabase row for P/N 1001530 "ARROW LABEL INSULATED BOOM," flagged because it has no `items/*.md` file, no category table row, and no `.claude/ARCHITECTURE.md` catalog entry. Queried `elliott_items`, `elliott_items_internal`, and `elliott_audit_log` in full. **Finding: this is NOT debris — it is a complete, real, quoted item that was priced through the account's own calculator/frontend tooling and written to Supabase, but never synced to the repo's markdown files.**

**Key Findings:**
- `elliott_items` row is fully populated: 15.875" × 2.375" = 0.262 sq ft, Orajet 3951 Cast + Polyester Lam, Print/Lam/Cut (1 pass), full 6-tier pricing ($7.50/$6.00/$4.50/$4.25/$4.25/$4.25), material cost $0.52/label, margin ~88.4% at qty 20, status "Quoted," date_quoted 2026-07-21.
- `created_at` = `updated_at` = **2026-07-21 20:24:58 UTC** — created one day before this session, not stale.
- `elliott_items_internal` row contains a full Pricing Derivation narrative: complete 4-wave AI validation record (Wave 1 build, Wave 2 destruction round that rejected a step-down tail for breaching §30 and §32, Wave 3 unanimous buyer-simulation approval, Wave 4 unanimous YES with zero changes), and a §32 chain-consistency check already run against P/N 1001220 and P/N 3017572 (flagging a 1-9 tier gap against 3017572, same class as the account's other flagged-not-corrected §32 gaps).
- `elliott_audit_log` confirms an `insert` on `elliott_items`, `changed_by: "frontend"`, at the same timestamp — created through the account's own tooling, not a manual/test insert. Two `elliott_materials` rows' `used_in_items` arrays were updated in the same transaction to include "1001530" — the Supabase-side material cross-ref bookkeeping is correct.
- No reference to 1001530 exists anywhere in the repo (`items/*.md`, `categories/*.md`, `ARCHITECTURE.md`, `PROGRESS.md`, `materials/*.md` `used_in_items` arrays) — confirmed via repo-wide grep. The item sits conceptually between P/N 1001220 (0.231 sq ft) and P/N 3017572 (0.365 sq ft) on the sub-scope gradient (documented as the "eleventh sub-scope data point" in its own Supabase notes), but its own governance file was never authored.
- `spec_sheet_paths` is empty — no drawing/spec sheet reference on file for this item.

**Action taken:** **None.** Per explicit session instruction, since real history/usage exists (a full validation record, not a bare row), the row was **not deleted** from `elliott_items` or `elliott_items_internal`, and no retroactive item file was auto-generated. This session stops here for Nick's review.

**Strategic Flags:**
- This reveals a process gap: a prior session (or a direct calculator run) wrote a fully-priced, 4-wave-validated item straight to Supabase without the corresponding repo-side item file / category update / ARCHITECTURE.md entry / materials cross-ref sync ever happening. Worth a process check on whether other orphan rows exist beyond 1001530.
- Nick's input needed: confirm the spec details (no spec sheet on file), then either author `items/1001530.md` (+ category file + ARCHITECTURE.md + materials cross-refs) from the existing Supabase record, or provide corrected specs if the on-file data is wrong.
- Do NOT delete this row — it is real, quoted, validated data.

**Status:** Investigation complete, no file or database changes made. Awaiting Nick's direction on how to reconcile 1001530 into the repo.

---

### 2026-07-22 — Session BD (governance fix): §32 amended to scope chain-consistency to same-corridor pairs; P/N 1257750's flag re-verified as legitimate, not a false positive

**What:** `governance/PRICING_RULES.md` §32 (nearest-neighbor chain consistency) was firing on cross-corridor comparisons within Band A cut vinyl — comparing the Small-Format Premium Corridor (items <~1.2 sq ft, deliberately priced $14.33–$14.36/sq ft) against the Relationship Concession Corridor (items ≥~1.5 sq ft, $13.65–$13.94/sq ft) treats a deliberate, validated structural gap as an inversion. §32 amended to state explicitly that Direction B only applies to same-corridor pairs, with both corridors' current membership defined in the rule text. `scripts/validate.py` was checked for enforced §32 logic — there is none; §32 exists only as prose in the `BAND_EXCEPTIONS` dict and in item-file Pricing Derivation sections, so no code change was needed (confirmed via a clean `python scripts/validate.py` run, 0 errors/0 warnings, unchanged from before this session).

**Key Decisions:**
- **Re-ran the §32 check against P/N 1257750 (1.076 sq ft) under the amended corridor rule — the flag against P/N 3010722 (1.167 sq ft) is NOT exempted.** Both items sit inside the Small-Format Premium Corridor (<1.2 sq ft) — this is a same-corridor pair, not the cross-corridor case the amendment targets. Confirmed the violation is real and correctly scoped, not a false positive; `items/1257750.md` updated (frontmatter `pricing_logic`, Pricing Derivation, Notes and Warnings) to document the re-check and its outcome — the flag itself was **not removed**, since it legitimately applies within-corridor.
- The originating session brief assumed 1257750 vs. 3010722 was the false-positive case this fix would resolve; verifying against the corridor definitions in §32 itself showed that assumption was wrong — 3010722 is a small-format-premium item, not a concession-corridor item, so no cross-corridor exemption applies here. No actual false-positive instance was identified or corrected this session; the fix is prospective (protects against a real class of error going forward) rather than resolving the specific case named in the brief.
- No item file other than `items/1257750.md` was touched. No pricing was changed on any item — only the §32 documentation and the 1257750 flag annotation.

**Strategic Flags:**
- The 1257750 vs. 3010722 §32 violation remains open and unresolved, exactly as before this session — still Nick's call per the flagged-not-corrected precedent (joining 3023921 on that list).
- Future items filed in the ~1.2–1.5 sq ft gap between the two corridors have no corridor home yet and need independent 4-wave validation before §32 can be run against them in either direction.

**Status:** Complete — `governance/PRICING_RULES.md` §32 amended with corridor definitions; `scripts/validate.py` confirmed to have no enforced §32 logic (none added, none needed); `python scripts/validate.py` — 0 errors, 0 warnings.

---

### 2026-07-22 — Session BC (new item): P/N 1257750 — LBL-V63 MED WHT, direct price replication of P/N 3010736, §32 chain-consistency violation flagged

**What:** New Vinyl Cut Lettering single label at 20" × 7.75" = 1.076 sq ft — sits between P/N 3010736 (1.012 sq ft) and the P/N 3010722/3010723/3010724 G50 color-parity set (1.167 sq ft) on the Band A gradient. Per explicit session instruction, this item's tier table is a **direct, byte-identical replication of 3010736's filed prices** ($19.75/$17.25/$14.50/$13.50/$13.25/$13.00) — not scaled by the sq ft ratio between the two items (1.076/1.012 = 1.063), and not independently re-validated. No new 4-wave AI validation was run. `override_type: "Owner Judgment"`.

**Key Decisions:**
- **Flagged §32 chain-consistency violation, not corrected.** Because the dollar price was held flat across a larger sq ft than the 3010736 benchmark, $14.50 at qty 20 ÷ 1.076 sq ft = **$13.48/sq ft** — below the $13.65–$13.94/sq ft concession corridor and below this item's nearest larger validated Band A neighbor, P/N 3010722 (1.167 sq ft, $14.36/sq ft). Checked against `governance/PRICING_RULES.md` §32 (Direction B — smaller item's $/sq ft must be at or above its nearest larger validated neighbor's, at every tier): **all six tiers fail** — the most complete §32 violation on file to date, wider than P/N 3023921's partial (2-tier) gap. Per this session's explicit instruction not to scale or re-derive the price, this is documented in `items/1257750.md` and `categories/cut-vinyl-3m-180mc.md`, not corrected.
- **Not counted as a new independent Band A data point.** Band A's confirmed data-point count stays at 12 — this is a price-replication filing, not a validated $/sq ft figure, mirroring how direct-parity clones (3020336, 3020482, 3020370) are excluded from data-point counts.
- Material cost $2.71/label computed fresh this session from the session spec sheet's stated rates (24" × 10yd White roll at $13.116/yd — the higher-cost-scenario roll per `governance/STRUCTURE_RULES.md`'s convention, since P/N 3010736 used the 50yd production roll — plus TransferRite 582U tape, length-based method). Unlike 3010736, this cost was **not** independently re-verified against the committed calculator engine this session — flagged as a working figure.
- `.claude/ARCHITECTURE.md` catalog row + Category Registry updated (Cut Vinyl 16 → 17 items; Band A data points unchanged at 12, with 1257750 explicitly excluded and cross-referenced). `categories/cut-vinyl-3m-180mc.md` items table row (inserted between 3010736 and the G50 set to preserve sq ft ordering) + Band A prose flag added. `materials/3m-180mc-white-24in.md` and `materials/transferrite-582u.md` `used_in_items` arrays both updated.

**Strategic Flags:**
- **This item's price-replication method produced a real, comprehensive price-size inversion — the exact error pattern the account has repeatedly caught and fixed elsewhere** (e.g., the Wave 2 corrections on 3010722, 3020335, 3020336/3020482). Recommend a future targeted session either (a) accepts this as a documented one-off replication exception, joining 3023921 on the flagged-not-corrected list, or (b) directs a re-price that restores §32 compliance against 3010722.
- No first article requested or offered — do not reference FA anywhere in this file or in any Sean-facing communication. Order quantity unconfirmed with Sean — does not block sending.
- Item count: 58 → 59. Cut Vinyl category: 16 → 17 items. Band A data points: unchanged at 12 (replication filing excluded).

**Status:** Complete — item file, category file, ARCHITECTURE.md, STATE.yml, materials cross-refs updated; `scripts/validate.py` run (0 errors, 0 warnings). `scripts/migrate_to_supabase.py` NOT run in live mode — no `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` credentials available in this session environment (same deferral as Sessions AZ/BA/BB); Nick to run manually to bring `elliott_items` to 59 rows.

---

### 2026-07-16 — Session BB (new item): P/N 3023921 — LABEL D100i, new Band A interior data point, full 4-wave AI validation with zero changes, §32 chain-consistency gap flagged

**What:** New Vinyl Cut Lettering single label at 28.0625" × 8.5" = 1.656 sq ft — a new Band A interior data point sitting between P/N 3010698 (1.582 sq ft) and P/N 3018378 (2.512 sq ft). Full 4-wave, 6-model AI validation (24 total model responses, unanimous APPROVE) ran against the calculator's Round 1 brief and produced **zero changes** to the original six-tier table or the $6.71 material cost — filed as a clean validation, not a revision. The Round 1 brief was initially blocked by an undefined TransferRite 582U tape rate ($undefined/yd → $NaN); Nick supplied the correct rate from a Grimco listing (30" roll, $141.86/100yd = $1.4186/yd), which turned out to be identical to the account's existing `materials/transferrite-582u-30in.md` file — not a newly-sourced material. Filed tiers: $30.00/$25.00/$23.00/$20.00/$17.00/$14.00 — $23.00 at qty 20 = $13.89/sq ft, inside the active $13.65–$13.94/sq ft concession band.

**Key Decisions:**
- **Comp-data discrepancy caught and corrected.** The session brief's "nearest comps" table cited sq ft/price/override figures for P/Ns 3010707/3010708/3010709 and 3010698 that did not match the on-file item data (3010707/08/09 are actually Band C at 0.969 sq ft/$20.64/sq ft, not Band A at 1.452 sq ft/$13.77/sq ft as stated; 3010698 has no override, not an "Owner Judgment override" at 1.896 sq ft/$11.60/sq ft as stated) — the brief appears to have conflated this item with the unrelated P/N 1146650 ("40142 Cardinal Red Model Designation"). Verified actual figures used throughout the item file; the band-fit conclusion is unaffected.
- **§32 chain-consistency gap flagged, not corrected.** `governance/PRICING_RULES.md` §32 (established the same day as this filing) was checked against nearest larger validated neighbor P/N 3018378 (2.512 sq ft): the 10-19 tier is a real violation (this item's $15.10/sq ft vs. 3018378's $15.92/sq ft floor, ~5.5 increments short) and the 200+ tier is a borderline violation (~2 increments short). Per §32's own governing language ("closing a flagged gap is a pricing decision for Nick, not an automatic recalculation") and the explicit session instruction that the locked tier table not be modified without a new validation cycle, this is documented as a flag in the item file and category file, not auto-corrected.
- Internal-only 200+ walk-away floor of $11.50/label (set by Wave 4) logged via an INTERNAL NOTE callout in the item file — not published, not to be surfaced to Sean, use only if the incumbent underbids this specific SKU during the account transition.
- 200+ margin (~52.1%) and tier compression (53.3%) both fall outside `categories/cut-vinyl-3m-180mc.md`'s currently-documented Band A ranges (margin floor ~59–81.0%, compression 31.6–51%) — flagged in the category file prose rather than silently rewriting the numeric ranges, per this session's explicit instruction to report a band shift rather than auto-update it.
- `.claude/ARCHITECTURE.md` catalog row + Category Registry updated (Cut Vinyl 15 → 16 items, Band A data points 11 → 12). `categories/cut-vinyl-3m-180mc.md` items table row + Band A prose flag added. `materials/3m-180mc-cardinal-red.md` and `materials/transferrite-582u-30in.md` `used_in_items` arrays both updated.

**Strategic Flags:**
- **This item's own filed price table is locked and correct per its 4-wave validation, but the account's own newly-established §32 rule (adopted the same day) hasn't been tested against it.** Recommend a future targeted session either accepts this as a documented exception (joining 1101250/3024595/3017572 on the "true one-off" list in `governance/PRICING_RULES.md` §32) or re-validates the 10-19 and 200+ tiers specifically.
- Do not cite the internal $11.50 200+ floor in any Sean-facing communication.
- FA and order quantity unconfirmed with Sean — does not block sending.
- Item count: 57 → 58. Cut Vinyl category: 15 → 16 items. Band A data points: 11 → 12.

**Status:** Complete — item file, category file, ARCHITECTURE.md, STATE.yml, materials cross-refs updated; `scripts/validate.py` (0 errors/0 warnings, band membership passes with no BAND_EXCEPTIONS entry needed), `scripts/profile.py` (confirms the Band A 200+ margin floor shift to 52.1%, flagged not auto-applied), and `scripts/build_frontend.py` (58 items) all run this session. `scripts/migrate_to_supabase.py` NOT run — no `.env`/service-role credentials available in this session environment, same deferral as Sessions AZ/BA; Nick to run manually to bring `elliott_items` to 58 rows.

---

*Entries older than Session BB (2026-07-16) were removed per the 10-entry rolling window — git history retains them in full.*
