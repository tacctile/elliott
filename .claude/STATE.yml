# Elliott Equipment — Session State
# Updated at the end of every session

last_session:
  date: "2026-06-01"
  type: "system"
  summary: "Session 2 — Calculator Core Logic Engine. Added a self-contained pricing engine to frontend/index.html as a <!-- CALCULATOR ENGINE --> <script> block before </body>. Pure logic, zero UI. Reads window.CALCULATOR_CONFIG (loaded via the existing init() — extended with one extra fetch for calculator_config.json) and window.ITEMS_DATA. Exposes window.runCalculator(inputs) returning a structured CalculatorResult per spec. Also exposes window.runSanityChecks() for console-side verification. Engine implements: routing (cut_vinyl / single_standard / single_sub_scope / kit / tiny / no_profile / stop), length-based cut-vinyl material cost (canonical + alternate-roll efficiency scenarios), print/lam material cost (canonical kit totals at $2.99/$5.16 for 3/5-label same-dim configurations; computed otherwise), lamination-pass derivation against the 13.5\" laminator and 28\" Roland print bed (F3/F20 STOP flags), tier construction (printed/lam singles use anchor_psf×sq_ft snap-rounded then ratio-snapped; printed/lam kits use the 3-label/5-label templates or per-label parity cost-build; cut vinyl uses active-band midpoint × sq ft with default-tier-template ratios; tiny flattens to the $55 account floor), MOQ 10 = $55 flat charge at the 1-9 tier on printed/lam items, never-pay-more enforcement (records every adjacent-pair cliff; auto-fixes only the customer-facing 10-19 → 20-49 boundary with ceil-snap per spec, skips the check entirely for cut vinyl), per-tier margin computation, band positioning with 5-cent tolerance for at_floor/at_ceiling classification, comparable-items lookup against data.json (same family + same item_type, ±15% sq ft, filtered by do_not_benchmark), 22 flag definitions (F1-F22) including F1/F2 material staleness, F3/F20 lam STOP, F4-F6 margin floors, F7 band tolerance, F8-F10 scope warnings, F11 cliff auto-fix, F12 ink unverified, F13 PMS caveat, F14 efficiency scenario, F15 Rule 14 deviation, F16 kit >2 lam passes, F17 no profile band, F18/F19 MOQ language, F21 mixed-dim kit, F22 sub-scope below-band, plain-text validation brief (everything Nick pastes into a 4-round AI validation prompt), quote-language stubs (anchor line + MOQ language + PMS caveat). ITEM/CATEGORY/GOVERNANCE FILES UNTOUCHED — engine-only session. validate.py 0/0. build_frontend.py / build_materials.py / build_calculator_config.py all clean. Both index.html script blocks parse without syntax errors."

next_action: "CALCULATOR ENGINE LANDED (2026-06-01, Session 2): runCalculator() in frontend/index.html. Session 3 will wire this to a Calculator tab UI. PRIMARY: Send $47.50 quote to Sean for 1210810 (qty 10, 10-19 tier at $4.75). Include in quote: (1) label dimensions: 'Label measures 10.5\" × 4\" (0.292 sq ft) in cast vinyl with polyester overlaminate — priced at $4.50 at your projected monthly volume.' (2) Standard MOQ language: 'Minimum order for printed labels is 10 units. Orders below 10 units are subject to a $55.00 minimum order charge. You will never be invoiced more for a smaller quantity.' Also: send $42 quote to Sean for 1082570 when PO arrives (job economics framing). Confirm 1082570 color selection via PO. Await Sean's outreach re: engineering/procurement standard project."
blockers: "Ink cost on 1210810 unverified — $0.25 is a placeholder for Safety Red flood coat; must confirm after first production run ($0.40–$0.50 realistic range) and update material_cost_per_unit in frontmatter. Open color conflict on 1082570 — Sean must select Safety Yellow or white background before production."
item_count: 15
material_count: 7
pending_quotes: "1210810 (new — $50.00 for qty 10), 1082570, 1245130, 3017435, 3018378, 1186310, 1277970, 1277980, 1277990, 1278000, 3017583, 3017584 — quoted May–Jun 2026, awaiting Sean response/PO"
