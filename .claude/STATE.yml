# Elliott Equipment — Session State
# Updated at the end of every session

last_session:
  date: "2026-06-05"
  type: "system"
  summary: "Session C: calculator engine rebuild completed in frontend/index.html (and frontend/calculator_config.json via scripts/build_calculator_config.py). Four new capabilities added: (1) enforceTierBoundaries() — a HARD constraint pass that runs after every tier builder, before margin calc, on every route that produces tiers (single_standard, single_sub_scope, kit, cut_vinyl). Walks boundaries bottom-up (200+ is the anchor), floor-caps any lower tier whose total at boundary_qty exceeds the upper tier's total at boundary_qty+1, with cascading — capped tiers become uppers for the next iteration. Each cap fires one F23 INFO flag with boundary-specific detail text (boundary qty, original price, capped price, cliff math). Skips tiny and no_profile. checkInvoiceProtection now runs as a secondary verification step and finds zero violations after enforceTierBoundaries. (2) Cut vinyl Band B (≥5 sq ft, $11.03/sq ft anchor from 3010704) and Band C (<1 sq ft, $20.64/sq ft anchor from 3010707 cluster) routing added to buildCutVinylTiers — when sq ft is within ±30% of the founding anchor sq ft, the validated 4-wave-AI tier template is used directly; otherwise the engine scales from anchor_psf × sq_ft and applies default ratios. Band A (small-format, concession_phase) is unchanged for the 1.0–4.99 sq ft range. (3) Sub-0.1 sq ft production override — a new production_override input on the calculator form (and gatherCalcInputs) routes Orajet 3951 items at sq ft ≤ 0.1 to single_sub_scope instead of tiny when true. OPT-IN; tiny remains the default. The override checkbox is auto-shown only for Orajet 3951 single items with sq ft ≤ 0.1, and forced unchecked when hidden. (4) 1-9 auto-generation at 1.5× was already in place from Session B; verified that enforceTierBoundaries auto-caps it when the 1.5× ratio creates a 9/10 inversion. F23 added to FLAG_DEFS. makeFlag extended with an optional detail field. generateFlags signature extended with tier_enforcement param. determineRoute updated with size-class note in route_reason for cut vinyl. getActiveBandRange now returns Band B/C psf when sq ft falls in the corresponding size class. computeBand passes ref_sq_ft to getActiveBandRange. CRITICAL FIX: both enforceTierBoundaries and checkInvoiceProtection now round boundary totals to penny precision before comparison, to eliminate floating-point false positives (e.g., 19 × 0.80 = 15.200...1 vs 20 × 0.76 = 15.2 in IEEE 754). Without this, checkInvoiceProtection spuriously fired F11 on compliant tier tables. UI: production override checkbox added between ANSI and Order Quantity fields; updateCalcFormVisibility wires change listeners for width/height to dynamically show/hide the override. runSanityChecks extended with three new test cases: 3010704 (Band B), 3010707 (Band C), and PROD-OVERRIDE-TEST (synthetic Orajet 8x1.75 with production_override:true). build_calculator_config.py: added ROUTING.cut_vinyl_band_thresholds (band_b_floor_sq_ft:5.0, band_c_ceiling_sq_ft:1.0); added CUT_VINYL_LETTERING_BAND.large_format (anchor_psf:11.03, anchor_pn:3010704, tier_template:105/92/78/68/60/52) and .sub_1_sqft (anchor_psf:20.64, anchor_pn:3010707, tier_template:28/24/20/16.50/13.50/11.50). calculator_config.json regenerated. governance/CALCULATOR.md updated: F23 added to Section 3 flag table; Rule 11 added to Section 4 (enforceTierBoundaries documentation + checkInvoiceProtection becoming a verification step); Section 2 Route Definitions cut_vinyl row now describes size-class routing; Section 2 Sq Ft Thresholds adds cut_vinyl_band_thresholds entries; Section 6.1 constant-change list adds cut vinyl band thresholds and Band B/C anchor/template; Section 9 Sanity Check Reference Cases rewritten with post-enforcement prices, all existing cases plus three new Band B/C/override cases, and a note explaining that the calculator's brief output may differ from catalog tier values (catalog retains upper-tier cliffs handled by §26 invoice protection; brief generator hard-enforces every boundary). Sanity matrix verified — all 11 routes match (route_match: true for every case). Both <script> blocks parse cleanly (Node new Function check). python scripts/validate.py — 0 errors, 0 warnings. All 3 build scripts run clean. NO item files touched. NO category files touched. NO governance/PRICING_RULES.md or MASTER_CONTEXT.md or ARCHITECTURE.md touched. NO flag definitions F1-F17, F20-F22 modified — F23 added only. Tiny route behavior preserved ($55 flat across all 6 tiers; F9 still fires)."

next_action: "No required next session. Session C's scope is complete and shipped. Open backlog items (not blocking): (a) governance/CALCULATOR.md Section 3 F18/F19 doc cleanup — cosmetic; (b) $100 minimum for Sean rush/favor jobs — undocumented; (c) validation-prompt augmentation backlog from prior sessions: W4 augment validation brief with production process step-by-step + full benchmark tier tables + key comparisons; W1 fix 5-label same-dim lam pass count; W2 add input-sanity STOP flag for width=0/height=0/label_count=0; W3 re-route or flag when item_type=kit but label_count=1; W6 add STOP flag when cut vinyl color not in config; W5 read fm.override_type in inferOverrideType; I2/I3 prose drift cleanup. (d) DESIGN QUESTION (not required to ship — Session C is correct per spec but the math is aggressive): consider whether to relax the strict cascade enforcement or re-tune the printed/laminated upper-tier ratios (0.85/0.70/0.55) so they don't create cliffs at 49/50, 99/100, 199/200 boundaries. Current strict implementation compresses tier tables substantially for the brief generator — the catalog tier values in items/*.md are unaffected because the calculator doesn't write to those files. This is a brief-generator-only design question. (e) Quote a new cut vinyl item at the Band B-A boundary (~4.5–5.5 sq ft) or Band A-C boundary (~0.8–1.2 sq ft) to validate the routing tree under boundary conditions before next non-boundary item."

blockers: "none — Session C complete. Calculator engine, calculator_config.json, and governance/CALCULATOR.md are in full alignment. Open backlog items are not blockers."

item_count: 19

pending_quotes: "3010707 ($20 for qty 20, Cardinal Red, Band C founding anchor), 3010708 ($20 for qty 20, Black, Band C color parity), 3010709 ($20 for qty 20, White, Band C color parity), 3010704 ($78 for qty 20, Band B founding), 1210810 (revalidated 2026-06-01; 1-9 tier $7.25 added 2026-06-05 in Session A MOQ purge — $57.50 for qty 10 at 10-19 tier of $5.75, recurring $4.75 at 20-49; §26 invoice protection resolves the 9/10 and 19/20 cliffs automatically), 1082570 ($42 flat for qty 2 once PO arrives; color selection pending; production rate $8 at qty 20 also valid), 1245130, 3017435, 3018378, 1186310, 1277970, 1277980, 1277990, 1278000, 3017583, 3017584 — quoted May–Jun 2026, awaiting Sean response/PO"
