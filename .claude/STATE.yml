# Elliott Equipment — Session State
# Updated at the end of every session

last_session:
  date: "2026-06-05"
  type: "system"
  summary: "Session B: calculator engine MOQ cleanup completed in frontend/index.html. Stripped all MOQ logic from the calculator engine to bring it into alignment with the Session A governance/category/item/build-script purge. Removed: F18 + F19 flag definitions from FLAG_DEFS, all F18/F19 firing branches across tiny / printed-laminated singles + sub_scope + kit / cut_vinyl routes, the $55 flat-tier injection at price_1_9 in buildPrintLamSinglesTiers and buildPrintLamKitTiers, the moq_applies parameter on those two builders + on checkInvoiceProtection, the 1-9 carve-out in checkInvoiceProtection (so invoice protection now checks ALL boundaries including 9/10), the moq_applies variable in runCalculator, the moq_applies / moq_charge fields on the pricing result object + on the sanity-check exposure, the .calc-moq-row CSS rule and the MOQ row rendering block in renderCalcPricingTable (1-9 now renders as a standard tier row), the moq_line quote-stub in generateQuoteStubs and its display in the brief, the MOQ line in the brief output. Updated route-reason text on cut_vinyl (dropped MOQ clause) and on tiny ('minimum-worthwhile-charge floor' → 'one-off job-economics floor'). The tiny route still flattens all 6 tiers to $55 (preserved as one-off job-economics); F9 still fires. Both script blocks parse cleanly. Sanity matrix verified — all 7 reference cases (1230820 single_standard $20, 1082570 single_standard $7.75/algorithmic-snap, 1210810 single_sub_scope $4.50, 1278930 kit $30, 1205720 cut_vinyl $35, 1277970 tiny $55, 1245130 kit $50) plus Convex no_profile case all produce correct route + correct price; no F18 or F19 in any output; all other flags (F8, F9, F10, F11, F12, F15, F17) unchanged. invoice_protection now correctly surfaces the 9/10 cliff on 1210810 (calculator algorithmic 1-9 = $6.75 from $4.50 × 1.5 snap; the catalog-locked 1-9 from Session A is $7.25; expected divergence — calculator is the band-anchored brief generator, catalog is the AI-validated lock). Files modified: frontend/index.html (engine only — 60 lines removed, 14 edited), .claude/PROGRESS.md, .claude/STATE.yml. No governance docs touched. No item files touched. No category files touched. No build script source touched. Regenerated JSONs (calculator_config.json, data.json, materials.json) only differ by timestamp. validate.py 0/0 (19 items). All 3 build scripts clean. governance/CALCULATOR.md Section 3 still lists F18/F19 in its Flag Definitions table — cosmetic doc cleanup, no engine-behavior impact; flagged as a future-session backlog item."

next_action: "Session C — calculator engine rebuild with tier constraint logic, Band B/C routing for cut vinyl, sub-0.1 production routing override. Specific scope: (1) add Band B (≥5 sq ft large-format) routing in determineRoute() for cut vinyl — current engine treats all cut vinyl as Band A; first non-Band-A cut vinyl item routed through the calculator will mis-price without this; (2) add Band C (<1 sq ft sub-1 sq ft cluster) routing in determineRoute() for cut vinyl; (3) add cut vinyl Band B + Band C constants to scripts/build_calculator_config.py (and regenerate calculator_config.json); (4) add tier constraint verification logic that checks calculator-produced tier tables against the band ratios + structural compression bounds, surfacing any out-of-bounds tier; (5) add a sub-0.1 sq ft production routing override that distinguishes a one-off tiny job (current behavior — F9 + $55 flat) from a tiny item entering production volume (would need a real per-unit rate); (6) reconcile the calculator-vs-catalog 1-9 divergence for 1082570 ($7.75 algo vs $8.00 catalog) and 1210810 ($6.75 algo vs $7.25 catalog) — either tune the band anchor's snap behavior, document the expected divergence in CALCULATOR.md, or both. Independent backlog (not blocking Session C): (a) governance/CALCULATOR.md Section 3 F18/F19 doc cleanup — cosmetic; (b) $100 minimum for Sean rush/favor jobs — undocumented; (c) validation-prompt augmentation backlog from prior sessions: W4 augment validation brief with production process step-by-step + full benchmark tier tables + key comparisons; W1 fix 5-label same-dim lam pass count; W2 add input-sanity STOP flag for width=0/height=0/label_count=0; W3 re-route or flag when item_type=kit but label_count=1; W6 add STOP flag when cut vinyl color not in config; W5 read fm.override_type in inferOverrideType; I2/I3 prose drift cleanup."

blockers: "none — Session B complete. Calculator engine and governance are now in full alignment on MOQ removal. Session C work is well-scoped and can begin without external decisions."

item_count: 19

pending_quotes: "3010707 ($20 for qty 20, Cardinal Red, Band C founding anchor), 3010708 ($20 for qty 20, Black, Band C color parity), 3010709 ($20 for qty 20, White, Band C color parity), 3010704 ($78 for qty 20, Band B founding), 1210810 (revalidated 2026-06-01; new 1-9 tier $7.25 added 2026-06-05 in Session A MOQ purge — $57.50 for qty 10 at 10-19 tier of $5.75, recurring $4.75 at 20-49; §26 invoice protection resolves the 9/10 and 19/20 cliffs automatically), 1082570 ($42 flat for qty 2 once PO arrives; color selection pending; production rate $8 at qty 20 also valid), 1245130, 3017435, 3018378, 1186310, 1277970, 1277980, 1277990, 1278000, 3017583, 3017584 — quoted May–Jun 2026, awaiting Sean response/PO"
