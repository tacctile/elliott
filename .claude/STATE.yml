# Elliott Equipment — Session State
# Updated at the end of every session

last_session:
  date: "2026-06-01"
  type: "fix"
  summary: "C1 FIX — Restored kit-route never-pay-more check. Single-line targeted change in frontend/index.html: added 'tiers: kit_totals,' as the first field of buildPrintLamKitTiers() return object (line 3239). The existing runCalculator guard `if (tierBuild.tiers && tierBuild.snap && route !== 'cut_vinyl')` now evaluates true for the kit route and calls checkInvoiceProtection() against kit_totals. F11 (never-pay-more cliff auto-fix detected) now fires for kits when the customer-facing 10-19 → 20-49 boundary has a cliff. Four acceptance tests verified via Node vm-context test rig against the live engine block: (1) 1278930 3-label kit — route=kit ✓, price@20=$30 ✓, F11 fires ✓, 4 violations recorded (10-19 → 20-49 cliff $84 auto-fixed: 10-19 tier $36 → $32; 3 upper cliffs recorded, tolerated by invoice protection language per spec). (2) 1245130 5-label kit — route=kit ✓, price@20=$50 ✓, F11 fires ✓, 4 violations recorded (10-19 cliff $140 auto-fixed: $60 → $55 ceilSnap with above_50 gran=5; residual $45 sub-snap cliff accepted per spec — matches 1210810 pattern). (3) 1230820 single regression — identical to pre-fix (10-19 auto-fix $24 → $21.50, F18+F11). (4) 1205720 cut vinyl regression — identical to pre-fix (route='cut_vinyl' explicit skip; 0 violations; F19+F15). runSanityChecks() shows all 7 cases route_match=true; 1278930 and 1245130 now fire F18+F11 (previously F18 only). Beneficial side effect: kit band positioning now correctly computes this_item_psf ($16.42/sq ft = 'at_floor' of kit band) instead of returning 'out_of_scope' due to undefined tierBuild.tiers. F7 does not fire (0% deviation). validate.py 0/0; build_frontend.py 15 items; build_materials.py 7 materials; build_calculator_config.py 3+3+8 — all clean. NO item files, category files, governance docs, or build scripts modified (per session scope). frontend/data.json, frontend/materials.json, frontend/calculator_config.json regenerated (timestamp-only)."

next_action: "C1 CLOSED (2026-06-01). Calculator engine is now safe for live use on all 6 routes: cut_vinyl, single_standard, single_sub_scope, kit, tiny, no_profile. F11 (never-pay-more auto-fix) now fires correctly for kit route. NEXT FIX BACKLOG (from 2026-06-01 audit, in priority order): W4 — augment validation brief with production process step-by-step + full benchmark tier tables + key comparisons (PRICING_VALIDATION.md Round 1 input spec); W1 — fix 5-label same-dim lam pass count (engine returns 1, reality 2 due to Roland 28\" print bed); W2 — add input-sanity STOP flag for width=0/height=0/label_count=0; W3 — re-route or flag when item_type=kit_* but label_count=1; W6 — add STOP flag when cut vinyl color not in config; W5 — read fm.override_type in inferOverrideType; I2/I3 — prose drift cleanup (items/1277970.md line 167 says $0.35 but frontmatter is $0.24; ARCHITECTURE.md 1210810 status column has prose). PARALLEL QUOTING ACTIONS (unchanged): Send $47.50 quote to Sean for 1210810 (qty 10, 10-19 tier at $4.75) with MOQ language. Send $42 quote to Sean for 1082570 when PO arrives. Confirm 1082570 color selection via PO. Await Sean's outreach re: engineering/procurement standard project. The 4-round AI validation per governance/PRICING_VALIDATION.md remains mandatory for all new items — calculator is the Round 1 brief generator, not a replacement."
blockers: "Ink cost on 1210810 unverified — $0.25 is a placeholder for Safety Red flood coat; must confirm after first production run ($0.40–$0.50 realistic range) and update material_cost_per_unit in frontmatter. Open color conflict on 1082570 — Sean must select Safety Yellow or white background before production."
item_count: 15
material_count: 7
pending_quotes: "1210810 (new — $47.50 for qty 10), 1082570, 1245130, 3017435, 3018378, 1186310, 1277970, 1277980, 1277990, 1278000, 3017583, 3017584 — quoted May–Jun 2026, awaiting Sean response/PO"
