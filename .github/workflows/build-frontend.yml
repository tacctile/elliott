name: Build Frontend Data

on:
  push:
    branches: [main]
    paths:
      - 'items/**'
      - 'materials/**'
      - 'scripts/build_frontend.py'
      - 'scripts/build_materials.py'
      - 'scripts/build_calculator_config.py'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build data.json and sync images
        run: python scripts/build_frontend.py

      - name: Build materials.json
        run: python scripts/build_materials.py

      - name: Build calculator_config.json
        run: python scripts/build_calculator_config.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add frontend/data.json frontend/materials.json frontend/calculator_config.json frontend/images/
          git diff --cached --quiet || git commit -m "system: auto-rebuild frontend data + sync images"
          git push
